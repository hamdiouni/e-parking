
{% extends 'base.html.twig' %}

{% block title %}Welcome!
{% endblock %}

{% block stylesheets %}
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:400,700"/>
  <link rel="stylesheet" href="{{ asset('css/fontawesome.min.css') }}"/>
  <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}"/>
  <link rel="stylesheet" href="{{ asset('css/templatemo-style.css') }}">
{% endblock %}

 {% block body %}
   <body>

      <nav class="navbar navbar-expand-xl">
        <div class="container h-100">
          <a class="navbar-brand" href="{{ path('welcome') }}">
            <h1 class="tm-site-title mb-0"> Admin</h1>
          </a>
          <button
            class="navbar-toggler ml-auto mr-0"
            type="button"
            data-toggle="collapse"
            data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent"
            aria-expanded="false"
            aria-label="Toggle navigation"
          >
            <i class="fas fa-bars tm-nav-icon"></i>
          </button>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mx-auto h-100">
              <li class="nav-item">
                <a class="nav-link"href="{{ path('dash') }}">
                  <i class="fas fa-tachometer-alt"></i> Dash
                  <span class="sr-only">(current)</span>
                </a>
              </li>
             
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_reclamation') }}">
                  <i class="fas fa-shopping-cart"></i> Liste des reclamations
                </a>
              </li>
<li class="nav-item">
                <a class="nav-link" href="{{ path('abonnement_index') }}">
                  <i class="fas fa-shopping-cart"></i> Liste des abonnements
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('categorie_afficher') }}">
                  <i class="far fa-file-alt"></i> Liste des categories
                </a>
              </li>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('user_index') }}">
                  <i class="far fa-user"></i> Profil d'administration
                </a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('vehicule_index') }}">
                  <i class="far fa-file-alt"></i> Liste des véhicules
                </a>
              </li>
               <li class="nav-item">
                <a class="nav-link" href="{{ path('app_parking_space_index') }}">
                  <i class="far fa-file-alt"></i> Liste des parking
                </a>
              </li>
              
            
            </ul>
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link d-block" href="{{ path('logout') }}">
                  Admin, <b>Logout</b>
                </a>
               
              </li>
            </ul>
          </div>
        </div>
      </nav>
      
 </body>
{% endblock %}

